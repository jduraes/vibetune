31-May-2026

# VibeTune Handoff

## 1. Session Purpose (completed)

1.1 Fix M9 hardware detection crash on SC126.
1.2 Validate v0.0.38 on real hardware (SC126 and RC2014).
1.3 Update repository documentation with evidence.
1.4 Hand off to first real PT3 decode slice (M5 continuation).

## 2. Completed Work Summary

2.1 M9 hardware detection (v0.0.38)
2.1.1 HBIOS ident via pointer at $FFFC (`'W'` / `~'W'`), `BF_SYSVER`, `BF_SNDQ_DEV`.
2.1.2 Fallback probing: Tune-compatible RIN readback, Z180 EB before MSX/RC/Coleco.
2.1.3 MSX port assignment corrected ($A0 reg select, $A1 data).
2.1.4 Hardware telemetry prints immediately after detection (before parse).

2.2 Hardware validation (user-confirmed, 31-May-2026)
2.2.1 SC126 + EB module: `Hardware: RCZ180 EB module ($68/$60)` — usage, rl2wof (AY), rl2wofts (TurboSound), -list (3 tracks); quit via Esc/Q/Ctrl-C returns to CCP cleanly.
2.2.3 v0.0.41 `vtune rl2wof`: stays in playback loop (silent until Phase C decode); user-confirmed Esc returns to prompt — not an immediate post-telemetry exit.
2.2.2 RC2014 + MSX-addressed card: `Hardware: MSX standard ($A0/$A1)` — intentional profile test; rl2wofts TurboSound line confirmed.

2.3 Build: vtune.com 4,306 bytes, v0.0.38.

## 3. Current Milestone State

3.1 M0–M4, M6, M7: complete.
3.2 M9 hardware detection: complete and validated on two physical profiles.
3.3 M8: auto-detect and routing prep complete; decode integration pending.
3.4 M5: in progress — loader/init/tick plumbing present; **real PT3 decode is the active gate**.

## 4. Current Truths to Preserve

4.1 TurboSound is file-driven, not user-toggled.
4.2 vtunecfg.com owns VTUNE.CFG authoring.
4.3 Startup telemetry (hardware + audio mode) does not imply audible decode parity.
4.4 SIMH content path is `Sync-SIMH-Content.ps1` (see SIMH-Testing.md §4).

## 5. Primary Next Work — PT3 decode slice (clean-room)

5.1 Replace proof-of-pipeline tick with original VibeTune PT3 decode in pt3engine.inc.
5.2 Use reference players and corpus only for format/compatibility understanding — no source transplant.
5.3 Keep tempo/quark pacing in shared main loop; decode emits register writes via PSG_WRITE_ROUTED.
5.4 First acceptance: audible structured playback on rl2wof.pt3 (AY) and rl2wofts.pt3 (TurboSound) on target hardware (startup paths already validated for both).
5.5 Record decode-smoke in TunesValidationLedger.md when audible parity is confirmed.
5.6 MYM full decode follows PT3 baseline.

## 6. Reference Artifacts

6.1 Project goals: ProjectGoals.md.
6.2 Milestones: ImplementationPlan.md.
6.3 Engine contract: PlaybackEngineSkeleton.md.
6.4 Parser/startup: ParserBehaviorSpec.md.
6.5 Validation evidence: TunesValidationLedger.md.
6.6 SIMH workflow: SIMH-Testing.md (canonical sync: Sync-SIMH-Content.ps1).

## 7. Last Known Version Snapshot

7.1 Runtime: v0.0.45 (31-May-2026) — PT3 engine init moved to after `LOAD_MUSIC_FILE` (was binding patterns in empty/garbage `MUSIC_BUF`).
7.2 v0.0.44: silent play + 2nd-run instant exit traced to pre-load `PT3_ENGINE_INIT`; AY silence uses `$0F` not `0`.
